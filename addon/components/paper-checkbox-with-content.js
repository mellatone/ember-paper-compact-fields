import Ember from 'ember';
import layout from '../templates/components/paper-checkbox-with-content';

export default Ember.Component.extend({
  layout,

  classNames: ['paper-checkbox-with-content']
});
